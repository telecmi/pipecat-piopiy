# Listing pipecat-piopiy in the Pipecat docs

Per pipecat-ai/COMMUNITY_INTEGRATIONS.md. Fork https://github.com/pipecat-ai/docs and:

1. `api-reference/server/services/supported-services.mdx` - add the row in
   `supported-services-row.md` to the table (telephony / transports section).
2. Create `api-reference/server/services/telephony/piopiy.mdx` from
   `piopiy.mdx` here.
3. Add the page path to `docs.json` navigation and a redirect entry.
4. Open the PR with the demo video link (30-60 s: a call answered, a warm
   transfer, an interruption), then post in `#community-integrations` on the
   Pipecat Discord.
